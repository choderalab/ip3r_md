# Equilibration

This directory contains simulation inputs, scripts, and other files generated by CHARMMGUI Solution Builder. 

To run energy minimization and equilibration:

1. Download CHARMM topology and parameter files from https://charmm-gui.org/?doc=toppar into a directory called `Toppar`. Ensure that that this directory is pointed to by `toppar.str`.
2. 
